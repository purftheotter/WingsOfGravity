[package]
name = "Wings_Of_Gravity"
version = "0.1.0"
edition = "2024"

[dependencies]
sdl2 = {version = '*', features = ["image","ttf"]}
rapier2d = { version = "*", features = [ "simd-stable", "serde-serialize" ] }
noise = "*"
serde = {version = '1', features = ["derive"]}
ron = "0.8"

